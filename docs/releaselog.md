# 3dprinter — Release Log / Uitgavelog

Short bilingual index. One row per tagged release. Detailed per-version notes live in `release-notes/vX.Y.Z.md`.

| Version | Date / Datum | Summary (EN) | Samenvatting (NL) |
|---------|--------------|--------------|-------------------|
| v0.4.0  | 2026-05-24   | Installable native app via **Tauri 2** (Windows `.msi`/`.exe`; Node helper bundled as a sidecar; read-only catalog/demo seed shipped as resources; same `%APPDATA%\Haspel\data` as dev). Plus: configurable **AI provider** (Claude CLI / Anthropic / OpenAI / Gemini / OpenRouter / none) with per-task models + self-test; per-install user data moved outside the repo; **filament history/archive** on removal; **on-spool vs refill** packaging split; full **end-user documentation** + production doc bundle; a clean **fresh-user test harness** (`stest`/`dtest`); Haspel branding + configurable label-sheet presets (Avery 6138) + brand logos + AMS-aware labels. | Installeerbare native app via **Tauri 2** (Windows `.msi`/`.exe`; Node-helper meegebundeld als sidecar; alleen-lezen catalogus/demo-seed als resources; zelfde `%APPDATA%\Haspel\data` als dev). Verder: instelbare **AI-provider** (Claude CLI / Anthropic / OpenAI / Gemini / OpenRouter / geen) met modellen per taak + zelftest; gebruikersdata per installatie buiten de repo; **filament-historie/archief** bij verwijderen; **op-spoel vs. navulling** verpakkingssplitsing; volledige **eindgebruikersdocumentatie** + productie-docbundel; een schone **nieuwe-gebruiker testomgeving** (`stest`/`dtest`); Haspel-huisstijl + instelbare labelvel-presets (Avery 6138) + merklogo's + AMS-bewuste labels. |
| v0.3.0  | 2026-05-11   | Closes Chunks E + F: PDF order import now actually runs end-to-end (helper-side `/import-order`), real Bambu-prioritised swatch resolver with multicolor stops, two-tier import-time swatch population (instant Tier 1 hint + async Tier 2 resolve), one-shot backfill of 13 legacy grey filaments, new Multicolor filter on the Filaments page, lenient JSON parser in the helper, Save-button + desktop-launcher fixes, shopping-list totals, extended P2S accessory catalog (33→62 parts + PTFE Tube) | Sluit Chunks E + F: PDF-orderimport werkt nu volledig (helper-zijde `/import-order`), echte Bambu-eerst kleurresolver met multicolor-stops, twee-traps kleurinvulling bij import (directe Tier 1 + asynchrone Tier 2 herresolutie), eenmalige backfill van 13 oude grijze filamenten, nieuw Multicolor-filter op de Filamenten-pagina, soepelere JSON-parser in de helper, fixes voor Opslaan-knop + desktop-snelkoppeling, totalen op boodschappenlijst, uitgebreide P2S-accessoirecatalogus (33→62 onderdelen + PTFE-slang) |
| v0.2.0  | 2026-05-08   | Filament catalogue (22 records / 26 spools), accessory CRUD, shopping list with catalog picker, PDF order import, filament detail modal, type/effect/color filters, batch AI fill, print+bed temp chips on cards, Vite ↔ helper lifecycle wiring | Filamentcatalogus (22 records / 26 spoelen), accessoires CRUD, boodschappenlijst met catalogus-picker, PDF-orderimport, filament-detailweergave, filters op type/effect/kleur, AI-batchverrijking, print+bed temperaturen op de kaarten, Vite ↔ helper levenscyclus-koppeling |
| v0.1.0  | 2026-05-08   | Initial release: filament inventory with manual swatches, AI usage lookup via Claude OAuth, helper service with heartbeat watchdog, taskbar launcher | Eerste release: filamentinventaris met handmatige kleurstaal, AI-gebruiksinformatie via Claude OAuth, helperservice met heartbeat-bewaking, taakbalk-snelkoppeling |
